# 简历可用表述

以下数字来自 `runs/screening/comparison-v2.json`，是冻结的单次 screening 结果。请结合自己实际参与、理解和后续开发情况使用。

## AI Agent 实习

- 实现工具调用型 Agent 回归评测工具，接入 OpenAI 兼容模型接口，构建30条正常、攻击与边界用例，记录工具名、参数、结果、Token和耗时，支持确定性断言、版本比较及JSON/HTML报告。
- 设计并实现 Fake Sink、随机 Canary、审批成对测试、HoneyTool与反事实重放；通过预植入泄漏和审批绕过验证检测链，使用独立审批绑定工具与完整参数。
- 从真实模型轨迹定位“未确认城市先调用天气工具”等问题；在同一冻结代码与30条用例下，Prompt修复后单次通过数由27增至30，新增退化0条，并保留评测器自身误判及修复复测记录。

## AI 产品实习

- 围绕 Agent 修改后缺少回归验证的问题，完成产品需求、目标用户假设、开发规范与可运行MVP，覆盖用例导入、运行、失败定位和版本决策流程。
- 设计任务完成率、工具约束、安全硬失败及成本警告指标，将30条真实模型评测结果整理为逐用例轨迹和版本比较报告，帮助解释“回答合理但过程违规”的问题。
- 完成一次由失败证据驱动的Prompt迭代：30条冻结用例中修复3条、无新增退化；同时记录平台自身格式误判的发现、修复与重新评测。

尚未开展外部用户研究，不写“验证市场需求”“服务多少用户”或“首次使用耗时降低多少”。尚未完成独立保留集多次实验，不写通用安全提升率或漏洞检出率。
